; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
lib_deps = 
    https://github.com/me-no-dev/ESPAsyncWebServer.git
    https://github.com/me-no-dev/AsyncTCP.git
    bblanchon/ArduinoJson@^6.21.3
    keypad
monitor_speed = 9600

; Optional: choose pins per device at build time.
; Set a sensor's pin to 0 to disable it for that firmware.
; Uncomment one of the envs below, or create your own.

; [env:car_alarm]
; extends = env:esp32dev
; build_flags =
;   -D VIB_PIN=15   ; vibration present
;   -D PIR_PIN=0    ; PIR disabled
;   -D GAS_PIN=34   ; gas sensor present

; [env:home_alarm]
; extends = env:esp32dev
; build_flags =
;   -D VIB_PIN=0    ; vibration disabled
;   -D PIR_PIN=18   ; PIR present
;   -D GAS_PIN=34   ; gas sensor present
